# Optional: a GitHub Personal Access Token (no scopes needed for public data).
# Without it the site still works, but adding one raises the API rate limit
# from 60 to 5,000 requests/hour — useful during frequent rebuilds.
GITHUB_TOKEN=
